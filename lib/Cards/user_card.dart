class UserCard {
  // Each card should have a name, an age, and a description
  String name;
  int age;
  String description;

  UserCard({required this.name, required this.age, required this.description});
}
